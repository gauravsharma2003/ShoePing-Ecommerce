<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-7">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
</head>
<body>
    <ul>
       <% for(let item of products){ %>
        <li>
            <img style="width:25%;" src="<%= item.img %>" alt="<%= item.name %>">
            <h3><%= item.name %></h3>
            <h4><%= item.price %></h4>
            <p><%= item.desc %></p>
        </li>
       <% } %>
    </ul>
</body>
</html>
